extends: ../paper/satedgesim_trace_mixed_v3_safe.yaml

ablation:
  name: no_gnn
  allowed_contract_diff_paths:
    - encoder.topology_encoder
    - encoder.temporal.enabled

model:
  topology_encoder: no_gnn
  temporal:
    enabled: false
