# Python: 3.11.8
# Docker image reference: Docker 26.1.0 + PyTorch 2.7.1 + TensorFlow 2.16.1
# Target: runnable TriSatFlow lightweight trainer/tests under trisatflow/, scripts/, tests/.
#
# Install from repo root:
#   python -m pip install --upgrade pip setuptools wheel
#   python -m pip install -r requirements.txt
#
# China mainland PyPI mirror acceleration.
-i https://pypi.tuna.tsinghua.edu.cn/simple
--trusted-host pypi.tuna.tsinghua.edu.cn

# Core DL stack aligned with BenchMARL 1.5.1 / TorchRL 0.10.
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1
tensorflow==2.16.1

# Numerical/config/runtime dependencies used by the TriSatFlow prototype.
numpy==1.26.4
scipy>=1.11,<1.15
PyYAML==6.0.2
tqdm==4.66.5
hydra-core==1.3.2
omegaconf==2.3.0
packaging==24.1
typing_extensions==4.12.2
protobuf==4.25.3

# Experiment result analysis / plotting convenience.
pandas==2.2.2
matplotlib==3.8.4

# BenchMARL-compatible auxiliary dependency from setup.py.
av>=10.0.0,<14.0.0

# Test dependency for smoke/unit tests in tests/.
pytest==8.3.2

# BenchMARL/TorchRL stack used by the upstream compatibility tests and adapters.
torchrl==0.10.0
tensordict==0.10.0
torch-geometric==2.8.0.post1
# vmas>=1.3.4
# pettingzoo[all]>=1.24.3
# moviepy>=1.0.3
# wandb>=0.17.0
