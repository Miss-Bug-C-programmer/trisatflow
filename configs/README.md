# Deprecated Config Wrappers

`configs/` is deprecated.

Canonical config root is:

```text
trisatflow/configs/
```

Files under `configs/` are compatibility wrappers that redirect to canonical
paths and trigger deprecation warnings in `load_config`.
